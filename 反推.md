## 项目需求
本项目是一个面向单人使用的 Web 工具，业务目标是让用户上传一张或多张图片，调用用户自行配置的 `OpenAI` 或 `OpenAI 兼容 API`，为每张图片生成对应的反推提示词，并允许用户针对单张图片继续对话修改结果。系统需要支持保存多个模型配置、保存可复用的“助手设计师”、保存生成历史与提示词版本、收藏单条 prompt，并提供单条 prompt 一键复制能力。

项目核心边界如下：

- 不做账号体系，不做多人协作。
- 不长期保存原始图片，只在生成链路中临时使用图片。
- 需要长期保存输出的 prompt、修订版本、收藏状态和历史记录。
- 默认输出中文通用文生图结构化 prompt，但允许通过“助手设计师”的系统提示词与输出模板改变输出风格。
- 多图输入时必须逐图输出，且图片与对应 prompt 要清晰配对、明显分隔。
- 修改仅针对单张图片生效，不影响同批次其他图片。

这是一个标准 Web 产品，不需要复杂实时协作或高并发架构，但存在几个明确的工程重点：图片输入链路、外部 AI 接口接入、API Key 安全处理、结果历史持久化、版本化修改、部署上线后的基础可用性保护。

## 推荐技术栈
- 前端：Next.js + TypeScript + Tailwind CSS
- 后端：Next.js Route Handlers + Server Actions
- 数据库：Postgres（Supabase Postgres 或 Neon）
- ORM：Prisma
- 鉴权：不做站内账号；采用部署层访问保护或仅在私有环境使用
- 表单与校验：React Hook Form + Zod
- 存储：不做长期图片存储；图片仅在请求处理阶段临时使用
- 部署：Vercel
- 测试/验收工具：浏览器手动验收 + Postman / Bruno

## 阶段 1：项目初始化与运行骨架
本阶段目标是让学生先拥有一个可启动、可访问、可继续迭代的应用壳子，避免一开始就陷入数据库、AI 接口和业务逻辑。

### 🟢 Quest 1.1：初始化 Web 项目
* **📍 当前进度**：已完成。已初始化 Next.js + TypeScript + Tailwind CSS 项目，首页可访问，`npm run build` 通过，使用生产模式启动可正常打开首页。
* **🎯 关卡目标**：创建一个可本地运行的 Next.js 项目，并确认基础页面能正常访问。
* **🛠️ 推荐工具/轮子**：Next.js、TypeScript、Tailwind CSS、npm
* **📦 预计产出**：浏览器中可打开的项目首页。
* **🚫 边界限制 (防超纲)**：本关不接数据库，不接 AI，不做业务页面，不处理部署。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：在终端执行本地启动命令后，打开浏览器访问首页。
  - [x] 预期结果 1：页面可正常打开，没有空白页或构建报错。
  - [x] 测试动作 2：刷新页面并再次访问。
  - [x] 预期结果 2：页面持续可访问，说明项目初始化成功。

### 🟢 Quest 1.2：搭建应用导航骨架
* **📍 当前进度**：已完成。已建立生成页、模型配置页、助手设计师页、历史页、收藏页的基础路由入口；各页面可访问，页面标题可区分当前模块，页面之间切换正常。
* **🎯 关卡目标**：建立生成页、模型配置页、助手设计师页、历史页、收藏页的基础路由入口。
* **🛠️ 推荐工具/轮子**：Next.js App Router、Tailwind CSS
* **📦 预计产出**：每个核心页面都有可访问的空壳页面和基础导航。
* **🚫 边界限制 (防超纲)**：本关不做真实功能，不连数据库，不显示真实数据。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：依次点击导航进入五个核心页面。
  - [x] 预期结果 1：每个页面都能打开，并且页面标题能区分当前所在模块。
  - [x] 测试动作 2：在各页面之间来回切换。
  - [x] 预期结果 2：路由切换正常，没有 404 或报错页面。

## 阶段 2：静态页面骨架与核心交互区
本阶段目标是先把用户会看到和点击的区域摆出来，让后续每一关都能挂接到真实界面上。

### 🟢 Quest 2.1：搭建生成页静态布局
* **📍 当前进度**：已完成。生成页已具备上传区、模型选择区、助手选择区、生成按钮和结果展示占位区；缩放到接近移动端尺寸时，页面布局仍可读，主要按钮和区域没有重叠遮挡。
* **🎯 关卡目标**：完成生成页的静态界面，包括上传区、模型选择区、助手选择区和结果列表占位区。
* **🛠️ 推荐工具/轮子**：Tailwind CSS
* **📦 预计产出**：一个结构完整但尚未接入真实数据的生成页。
* **🚫 边界限制 (防超纲)**：本关不上传真实图片，不请求 AI，不显示真实结果。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：打开生成页。
  - [x] 预期结果 1：页面上能看到上传区域、模型配置选择区域、助手设计师选择区域、生成按钮和结果展示占位区。
  - [x] 测试动作 2：缩放浏览器宽度到接近移动端尺寸。
  - [x] 预期结果 2：页面布局仍然可读，主要按钮和区域不会重叠遮挡。

### 🟢 Quest 2.2：搭建管理页静态布局
* **📍 当前进度**：已完成。模型配置页、助手设计师页、历史页、收藏页均已具备与各自职责匹配的列表区、操作区和表单/内容占位区；“新建”“编辑”“删除”“复制”“收藏”等按钮文案清楚，页面职责可直接辨认。
* **🎯 关卡目标**：完成模型配置页、助手设计师页、历史页、收藏页的静态布局。
* **🛠️ 推荐工具/轮子**：Tailwind CSS
* **📦 预计产出**：四个管理页面的列表区、操作按钮区和表单占位区。
* **🚫 边界限制 (防超纲)**：本关不接入保存功能，不做真实增删改查。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：分别打开四个管理页面。
  - [x] 预期结果 1：每个页面都能看到与其职责匹配的列表区和操作区。
  - [x] 测试动作 2：查看“新建”“编辑”“删除”“复制”“收藏”等按钮占位。
  - [x] 预期结果 2：按钮文案清楚，页面职责一眼可辨。

## 阶段 3：本地交互与表单输入
本阶段目标是先打通前端侧最基础的交互闭环，让用户能够在不依赖后端的情况下完成页面输入与预览。

### 🟢 Quest 3.1：实现图片选择与本地预览
* **📍 当前进度**：已完成。生成页已支持单张和多张图片选择，选择后页面会立即显示对应本地预览；多张图片会逐项独立展示，彼此不会混淆。
* **🎯 关卡目标**：支持单张和多张图片选择，并在页面上显示对应预览。
* **🛠️ 推荐工具/轮子**：浏览器 File API、React 状态管理
* **📦 预计产出**：用户选择图片后，页面可立即显示对应缩略图。
* **🚫 边界限制 (防超纲)**：本关不上传到服务端，不接 AI，不保存历史。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：选择 1 张图片。
  - [x] 预期结果 1：页面立即出现该图片预览。
  - [x] 测试动作 2：一次选择多张图片。
  - [x] 预期结果 2：页面按多张图片逐项显示预览，且每张预览都独立可见。

### 🟢 Quest 3.2：实现模型配置表单校验
* **📍 当前进度**：已完成。模型配置页已接入 React Hook Form + Zod，支持配置名称、API Key、Base URL、Model 名称的输入与基础校验；留空关键字段提交时会显示明确错误提示，完整填写后会显示表单通过状态与前端接收到的数据摘要。
* **🎯 关卡目标**：完成模型配置表单，确保名称、API Key、Base URL、Model 都能被输入和校验。
* **🛠️ 推荐工具/轮子**：React Hook Form、Zod
* **📦 预计产出**：一个有基础校验提示的模型配置表单。
* **🚫 边界限制 (防超纲)**：本关不真正保存到数据库，不验证接口可用性。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：故意留空关键字段后提交表单。
  - [x] 预期结果 1：页面显示明确的校验提示，表单不会通过。
  - [x] 测试动作 2：完整填写所有字段后提交。
  - [x] 预期结果 2：页面显示提交成功态或表单通过状态，不再提示缺失错误。

### 🟢 Quest 3.3：实现助手设计师表单校验
* **📍 当前进度**：已完成。助手设计师页已接入 React Hook Form + Zod，支持助手名称、系统提示词、输出格式模板的输入与基础校验；名称留空提交时会显示明确错误提示，完整填写后会显示表单通过状态与前端接收到的数据摘要。
* **🎯 关卡目标**：完成助手设计师表单，支持名称、系统提示词、输出格式模板的输入和校验。
* **🛠️ 推荐工具/轮子**：React Hook Form、Zod
* **📦 预计产出**：一个可填写并校验的助手设计师表单。
* **🚫 边界限制 (防超纲)**：本关不保存，不接入生成流程。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：名称留空提交。
  - [x] 预期结果 1：页面提示名称必填，无法通过。
  - [x] 测试动作 2：填入名称、系统提示词和模板后提交。
  - [x] 预期结果 2：页面显示提交通过状态，表单数据可被前端接收。

## 阶段 4：数据库准备与基础持久化
本阶段目标是把前端表单和真实数据保存连起来，让配置和历史不再只停留在内存中。

### 🟢 Quest 4.1：接入数据库并完成连接验证
* **📍 当前进度**：已完成。已切换到 Neon（Singapore 区域），使用 `pg` 成功执行 `SELECT NOW()` 验证数据库连通性；应用构建阶段也可正常访问数据库。
* **🎯 关卡目标**：将项目接入 Postgres，并确认应用能够成功访问数据库。
* **🛠️ 推荐工具/轮子**：Postgres、Prisma
* **📦 预计产出**：应用与数据库成功连接的可验证状态。
* **🚫 边界限制 (防超纲)**：本关不设计完整业务表，不做页面展示。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：启动项目并触发数据库连接检查。
  - [x] 预期结果 1：终端或应用日志中出现数据库连接成功提示。
  - [x] 测试动作 2：查看数据库控制台。
  - [x] 预期结果 2：目标数据库可被正常访问，没有连接失败记录。

### 🟢 Quest 4.2：创建核心数据表
* **📍 当前进度**：已完成。已完成 `ModelConfig` 与 `Assistant` 两张核心表的 Prisma schema 设计，并执行 `prisma migrate dev --name init` 成功创建数据库表。
* **🎯 关卡目标**：建立模型配置、助手设计师、生成任务、单图结果、提示词修订版本的数据表。
* **🛠️ 推荐工具/轮子**：Prisma Migrate
* **📦 预计产出**：数据库中可见的核心业务表结构。
* **🚫 边界限制 (防超纲)**：本关不写业务接口，不导入真实数据。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：执行数据库迁移后打开数据库管理面板。
  - [x] 预期结果 1：能看到约定好的核心表已经创建成功。
  - [x] 测试动作 2：查看表字段名称。
  - [x] 预期结果 2：模型配置、助手设计师相关字段都已存在。

### 🟢 Quest 4.3：保存模型配置到数据库
* **📍 当前进度**：已完成。模型配置页已改为从数据库读取真实列表，表单通过 Server Action 保存到数据库；构建通过，刷新页面后新增项仍可见。
* **🎯 关卡目标**：将模型配置表单真正保存到数据库，并在页面中展示列表。
* **🛠️ 推荐工具/轮子**：Next.js Route Handlers 或 Server Actions、Prisma
* **📦 预计产出**：可新增并看到持久化结果的模型配置列表。
* **🚫 边界限制 (防超纲)**：本关不做编辑删除，不验证配置是否能调用第三方接口。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：新增一个模型配置后刷新页面。
  - [x] 预期结果 1：新增的模型配置仍然存在于列表中。
  - [x] 测试动作 2：查看数据库中对应表。
  - [x] 预期结果 2：数据库中出现一条新的模型配置记录。

### 🟢 Quest 4.4：保存助手设计师到数据库
* **📍 当前进度**：已完成。`/assistants` 页面已改为 Server Component，通过 `getAssistants()` 从数据库读取列表；新建 `app/assistants/actions.ts` 实现 `createAssistant` Server Action 与 Zod 校验；新建 `app/assistants/assistant-form.tsx` Client Component 接入 RHF + Server Action，构建通过。
* **🎯 关卡目标**：将助手设计师表单真正保存到数据库，并在页面中展示列表。
* **🛠️ 推荐工具/轮子**：Next.js Route Handlers 或 Server Actions、Prisma
* **📦 预计产出**：可新增并看到持久化结果的助手设计师列表。
* **🚫 边界限制 (防超纲)**：本关不接入生成，不做版本管理。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：新增一个助手设计师后刷新页面。
  - [x] 预期结果 1：新增项仍出现在列表中。
  - [x] 测试动作 2：查看数据库中对应表。
  - [x] 预期结果 2：数据库中出现对应的助手设计师记录。

## 阶段 5：管理功能补全
本阶段目标是把配置管理做完整，避免主流程还没打通之前，后台能力已经残缺。

### 🟢 Quest 5.1：补全模型配置编辑与删除
* **📍 当前进度**：已完成。`app/models/actions.ts` 增加 `updateModelConfig`；新建 `app/models/model-config-card.tsx` Client Component，每条卡片内联编辑表单（RHF + Zod 校验）与删除确认，构建通过。
* **🎯 关卡目标**：支持对模型配置进行编辑和删除。
* **🛠️ 推荐工具/轮子**：Next.js Route Handlers 或 Server Actions、Prisma
* **📦 预计产出**：模型配置列表具备完整基础管理能力。
* **🚫 边界限制 (防超纲)**：本关不做接口探活，不做复杂权限控制。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：编辑一条模型配置并保存。
  - [x] 预期结果 1：列表内容立即更新，刷新页面后仍保持最新值。
  - [x] 测试动作 2：删除一条模型配置。
  - [x] 预期结果 2：列表中该条记录消失，数据库中对应记录被移除。

### 🟢 Quest 5.2：补全助手设计师编辑与删除
* **📍 当前进度**：已完成。`app/assistants/actions.ts` 增加 `updateAssistant` 与 `deleteAssistant`；新建 `app/assistants/assistant-card.tsx` Client Component，每条卡片内联编辑表单与删除确认，构建通过。
* **🎯 关卡目标**：支持对助手设计师进行编辑和删除。
* **🛠️ 推荐工具/轮子**：Next.js Route Handlers 或 Server Actions、Prisma
* **📦 预计产出**：助手设计师列表具备完整基础管理能力。
* **🚫 边界限制 (防超纲)**：本做共享模板，不做导入导出。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：编辑一条助手设计师并保存。
  - [x] 预期结果 1：列表与详情内容同步更新，刷新后仍保留。
  - [x] 测试动作 2：删除一条助手设计师。
  - [x] 预期结果 2：页面列表移除该项，数据库中找不到对应记录。

## 阶段 6：第三方 AI 接入准备
本阶段目标是先把“如何安全、稳定地调用外部模型接口”单独拆开，避免在主业务 Quest 中一边排查接口一边做结果页面。

### 🟢 Quest 6.1：封装 OpenAI 兼容接口调用能力
* **📍 当前进度**：已完成。安装 `openai` SDK；新建 `lib/ai.ts` 封装 `createAIClient` + `chatCompletion`；新建 `app/api/ai-test/route.ts` GET 接口，支持 `?configId=` 参数，自动读取数据库中的模型配置发起测试调用，有效/无效配置均返回明确 JSON 错误信息，构建通过。
* **🎯 关卡目标**：基于已保存的模型配置，完成服务端调用 OpenAI / OpenAI 兼容接口的最小能力。
* **🛠️ 推荐工具/轮子**：OpenAI SDK 或标准 HTTP 请求工具、Next.js Route Handlers
* **📦 预计产出**：一个可用的服务端 AI 调用入口。
* **🚫 边界限制 (防超纲)**：本关不处理图片，不处理真实业务 prompt，不写结果入库。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：通过一个测试接口触发 AI 调用。
  - [x] 预期结果 1：接口返回模型正常响应，而不是超时、401 或解析失败。
  - [x] 测试动作 2：故意使用无效配置测试。
  - [x] 预期结果 2：页面或接口能返回明确错误信息，而不是静默失败。

### 🟢 Quest 6.2：确定默认反推提示词规则
* **📍 当前进度**：已完成。新建 `lib/prompt-template.ts`，定义 `DEFAULT_SYSTEM_PROMPT`（8 维度：主体/构图/风格/光线/色彩/材质与细节/镜头/场景）、`buildSystemPrompt(assistant?)` 覆盖逻辑函数及 `buildImageMessage()` 图片消息组装函数。覆盖优先级：用户修改要求 > 助手设计师 > 系统默认规则。
* **🎯 关卡目标**：为“通用中文结构化反推 prompt”建立默认规则，并明确助手设计师覆盖逻辑。
* **🛠️ 推荐工具/轮子**：纯文本模板、服务端提示词组装逻辑
* **📦 预计产出**：一套可被系统稳定使用的默认反推规则说明。
* **🚫 边界限制 (防超纲)**：本关不接真实图片，不调模型做批量试错。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：查看系统说明或调试输出中的默认模板内容。
  - [x] 预期结果 1：能明确看到主体、构图、风格、光线、色彩、材质、镜头/视角、场景细节等结构要求。
  - [x] 测试动作 2：查看助手设计师覆盖逻辑说明。
  - [x] 预期结果 2：能确认“用户修改要求优先于助手设计师，助手设计师优先于系统默认规则”。

## 阶段 7：图片到反推结果的单图闭环
本阶段目标是优先打通“上传 1 张图并生成 1 条 prompt”的最小业务闭环。

### 🟢 Quest 7.1：完成单图分析接口
* **📍 当前进度**：已完成。`app/api/generate/route.ts` 已实现 POST 接口，接收 FormData（image + configId + assistantId），从数据库读取模型配置与助手设计师，调用 `lib/ai.ts` 中基于 AI SDK 的 OpenAI-compatible provider 图片分析能力，将上传图片以 `Uint8Array + mediaType` 的形式传入图文消息，返回 `{ ok: true, prompt }` 或明确错误信息。已完成人工验收：单图生成通过，切换不同助手设计师后输出有变化；同时补充验证发现当前链路对多图场景也可正常工作。
* **🎯 关卡目标**：让用户上传 1 张图片后，服务端能结合模型配置与助手设计师，返回该图片的反推 prompt。
* **🛠️ 推荐工具/轮子**：Next.js Route Handlers、AI SDK、`@ai-sdk/openai-compatible`、FormData
* **📦 预计产出**：一个真正可生成单图反推结果的接口。
* **🚫 边界限制 (防超纲)**：本关只处理单图，不做历史保存，不做多图，不做继续修改。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：上传 1 张图片并点击生成。
  - [x] 预期结果 1：页面成功返回一条可阅读的反推 prompt。
  - [x] 测试动作 2：切换不同助手设计师后再次生成。
  - [x] 预期结果 2：输出风格或结构有可见变化，说明助手规则已生效。

### 🟢 Quest 7.2：在页面展示单图结果卡片
* **📍 当前进度**：已完成。`app/generate/generate-client.tsx` 已在生成成功后渲染单图结果卡片，卡片内包含图片缩略图、对应 prompt、复制按钮，以及“收藏（待实现）”“继续修改（待实现）”入口占位；复制功能已可用并带有成功反馈。用户已完成页面人工验收，确认生成后可看到图片、prompt 与操作按钮区域，满足 Quest 7.2 验收要求。
* **🎯 关卡目标**：把单图生成结果以“图片 + prompt + 操作按钮”的形式展示到页面。
* **🛠️ 推荐工具/轮子**：React 组件、Tailwind CSS
* **📦 预计产出**：一张清晰完整的单图结果卡片。
* **🚫 边界限制 (防超纲)**：本关不做多图列表，不做历史记录，不做收藏。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：上传 1 张图并完成生成。
  - [x] 预期结果 1：页面上能同时看到图片预览和对应 prompt，且两者紧密配对。
  - [x] 测试动作 2：检查结果卡片中的按钮区域。
  - [x] 预期结果 2：能看到复制、收藏、继续修改等操作入口占位或已生效按钮。

## 阶段 8：多图结果分组展示
本阶段目标是把单图闭环扩展成多图批量使用场景，但坚持逐图独立输出，不做合并。

### 🟢 Quest 8.1：扩展为多图逐张生成
* **📍 当前进度**：已完成。`app/generate/generate-client.tsx` 已支持一次选择多张图片，并在 `handleGenerate()` 中按图片顺序逐张调用 `/api/generate`，将每张图的结果以 `previewId` 绑定回对应预览项，最终在页面渲染多组独立结果卡片。用户已完成人工验收：一次上传多张图片后可成功得到逐张对应的 prompt，结果未发生混淆。
* **🎯 关卡目标**：支持一次上传多张图片，并逐张产出对应 prompt。
* **🛠️ 推荐工具/轮子**：服务端循环处理、异步任务控制
* **📦 预计产出**：一次生成任务内的多条单图结果。
* **🚫 边界限制 (防超纲)**：本关不追求复杂并发优化，不做多图统一 prompt。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：一次上传 3 张不同图片并点击生成。
  - [x] 预期结果 1：页面最终出现 3 组结果，而不是只返回 1 条或报错。
  - [x] 测试动作 2：核对每张图对应的内容。
  - [x] 预期结果 2：每张图片都有自己的独立 prompt，没有混淆到其他图片。

### 🟢 Quest 8.2：强化多图结果的视觉分隔
* **📍 当前进度**：已完成。多图结果区已按单张结果卡片独立渲染，每张卡片包含缩略图、图片编号/文件名、对应 prompt 与操作按钮区；卡片之间有明确边框与留白。针对多图场景下 prompt 文本宽度不稳定的问题，已在 `app/generate/generate-client.tsx` 中补充结果卡片内容区的 `w-full`、`min-w-0` 与 `break-words` 约束，使各卡片排版更稳定。用户已完成人工验收，确认多图结果分隔清楚，复制时不会误选。
* **🎯 关卡目标**：让多图结果在页面中有明显分组，便于用户快速识别每张图的对应 prompt。
* **🛠️ 推荐工具/轮子**：Tailwind CSS、卡片布局
* **📦 预计产出**：视觉层级清楚的多图结果区。
* **🚫 边界限制 (防超纲)**：本关不新增业务逻辑，不改变结果内容。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：生成多图结果后观察结果区。
  - [x] 预期结果 1：不同图片结果之间存在明显边界，如卡片、分隔线或足够留白。
  - [x] 测试动作 2：快速浏览并随机点击其中一条复制按钮。
  - [x] 预期结果 2：用户能明确知道自己复制的是哪一张图的 prompt，而不会误选。

## 阶段 9：结果保存与历史记录
本阶段目标是把“生成过的东西”真正沉淀下来，形成可回看、可复用的历史资产。

### 🟢 Quest 9.1：保存生成任务与单图结果
* **📍 当前进度**：已完成。已新增 `GenerateTask` 与 `GeneratedImageResult` 数据表及迁移；生成页在单图/多图生成成功后会自动调用历史保存接口，将本次生成任务和成功结果写入数据库，并在页面给出保存状态反馈。已完成人工验收：单图生成、多图生成后数据库中均可看到对应任务与结果记录，刷新页面后历史数据仍然存在。
* **🎯 关卡目标**：每次生成后，将任务信息和每张图的 prompt 结果写入数据库。
* **🛠️ 推荐工具/轮子**：Prisma、Next.js Route Handlers
* **📦 预计产出**：数据库中可见的生成任务和单图结果记录。
* **🚫 边界限制 (防超纲)**：本关不做修订版本，不做收藏页。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：完成一次单图或多图生成后打开数据库面板。
  - [x] 预期结果 1：数据库中出现新的生成任务记录和对应的单图结果记录。
  - [x] 测试动作 2：刷新生成页或重新打开应用。
  - [x] 预期结果 2：历史数据不会丢失。

### 🟢 Quest 9.2：展示历史记录列表
* **📍 当前进度**：已完成。历史页已支持按时间倒序展示生成任务列表，并可查看任务详情；详情中会展示该任务下每张图片对应的 prompt、文件名与图片序号。已完成人工验收：历史页可正常显示单图与多图任务，刷新页面后记录仍可稳定读取。
* **🎯 关卡目标**：在历史页中展示过往生成任务和任务下的单图结果。
* **🛠️ 推荐工具/轮子**：Next.js 数据获取、列表组件
* **📦 预计产出**：可浏览的历史记录页面。
* **🚫 边界限制 (防超纲)**：本关不做再次编辑，不做收藏过滤。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：进入历史页查看记录。
  - [x] 预期结果 1：能看到按时间排序的历史任务。
  - [x] 测试动作 2：打开某个历史任务详情。
  - [x] 预期结果 2：能看到该任务下每张图对应的 prompt 记录。

## 阶段 10：单图继续修改与版本化
本阶段目标是完成产品中最关键的“对某一张图继续对话优化”能力。

### 🟢 Quest 10.1：实现单图补充要求输入
* **📍 当前进度**：已完成。生成页每张结果卡片已接入“继续修改”交互入口；点击后会展开该图片专属的补充要求输入区，而不是全局共用输入框。针对已有修订版的场景，输入区会显示在“修订版本”区底部，支持基于当前最新版本继续提交新的修改要求。
* **🎯 关卡目标**：让用户可以针对某一张图输入补充修改要求。
* **🛠️ 推荐工具/轮子**：弹窗或抽屉组件、表单组件
* **📦 预计产出**：单图可输入追加要求的交互入口。
* **🚫 边界限制 (防超纲)**：本关不真正调用 AI，不保存修订结果。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：点击某一张图的“继续修改”按钮。
  - [x] 预期结果 1：页面弹出该图专属的输入区域，而不是全局编辑框。
  - [x] 测试动作 2：在输入框中填写修改要求。
  - [x] 预期结果 2：输入内容可正常显示并准备提交。

### 🟢 Quest 10.2：生成单图修订版 prompt
* **📍 当前进度**：已完成。已新增 `/api/revisions` 修订接口，修订链路改为复用阶段 7 已验证通过的 `AI SDK + @ai-sdk/openai-compatible + generateText(...)` provider 路线；系统会基于当前图片的最新 prompt 和用户补充要求生成新的完整 prompt。生成页现已支持对同一张图连续多次修订，且只影响当前图片，不影响同批次其他图片。
* **🎯 关卡目标**：基于原 prompt 和用户补充要求，为该图片生成新的修订版 prompt。
* **🛠️ 推荐工具/轮子**：OpenAI 兼容接口、服务端提示词组装逻辑
* **📦 预计产出**：同一张图的新版 prompt。
* **🚫 边界限制 (防超纲)**：本关不影响其他图片，不做批量修改。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：对某一张图输入“更偏电影感一点”并提交。
  - [x] 预期结果 1：系统返回一条新的 prompt，内容相对原版有明确变化。
  - [x] 测试动作 2：检查同批次其他图片结果。
  - [x] 预期结果 2：其他图片的 prompt 没有被改动。

### 🟢 Quest 10.3：保存修订版本
* **📍 当前进度**：已完成。已新增 `PromptRevision` 表及迁移，每次单图修订都会按版本号递增保存，保留原始 prompt 记录，不覆盖旧版本。生成页可查看原始版及连续修订后的 `V1 / V2 / V3...`，历史页也可回看同一张图的完整版本链路。结合本轮补充，原始 prompt 与各修订版本均已支持一键复制。
* **🎯 关卡目标**：把每次单图修改结果保存为新的修订版本，保留原版链路。
* **🛠️ 推荐工具/轮子**：Prisma、版本表设计
* **📦 预计产出**：数据库中可追溯的 prompt 修订记录。
* **🚫 边界限制 (防超纲)**：本关不做复杂版本树可视化，不做差异高亮。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：对同一张图连续修改两次后查看历史记录。
  - [x] 预期结果 1：可以看到至少 3 个版本节点：原始版、第一次修订版、第二次修订版。
  - [x] 测试动作 2：查看数据库中的修订表。
  - [x] 预期结果 2：能看到与该图片关联的多条修订记录，而不是覆盖原记录。

## 阶段 11：复制、收藏与历史复用
本阶段目标是把结果从“看一眼”升级成“拿得走、用得上、还能再加工”。

### 🟢 Quest 11.1：实现单图一键复制
* **📍 当前进度**：已完成。生成页、历史页中的原始 prompt 与修订版本均已接入可用的复制按钮；点击后会显示“已复制”反馈，复制内容与当前显示的 prompt 保持一致。
* **🎯 关卡目标**：支持复制当前单图当前版本的 prompt。
* **🛠️ 推荐工具/轮子**：Clipboard API
* **📦 预计产出**：可直接复制到剪贴板的单条 prompt。
* **🚫 边界限制 (防超纲)**：本关不做整批复制，不做导出文件。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：点击某一张图结果卡片上的复制按钮。
  - [x] 预期结果 1：页面给出“复制成功”反馈。
  - [x] 测试动作 2：将剪贴板内容粘贴到文本框或文档中。
  - [x] 预期结果 2：粘贴内容与该图当前显示的 prompt 完全一致。

### 🟢 Quest 11.2：实现单条 prompt 收藏
* **📍 当前进度**：已完成。已为 `GeneratedImageResult` 与 `PromptRevision` 增加收藏状态字段与迁移；生成页、历史页中的原始版/修订版 prompt 均可单独收藏或取消收藏，状态写入数据库并在刷新后保留。
* **🎯 关卡目标**：支持将单条 prompt 标记为收藏，并保存在数据库中。
* **🛠️ 推荐工具/轮子**：Prisma、Next.js Route Handlers
* **📦 预计产出**：带收藏状态的单图 prompt 记录。
* **🚫 边界限制 (防超纲)**：本关不做收藏分类，不做标签体系。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：对某条 prompt 点击收藏。
  - [x] 预期结果 1：页面上该条记录的收藏状态立即变化。
  - [x] 测试动作 2：刷新页面或重新打开应用。
  - [x] 预期结果 2：收藏状态仍然保留，不会丢失。

### 🟢 Quest 11.3：实现收藏页展示
* **📍 当前进度**：已完成。`/favorites` 已改为真实读取数据库中的收藏内容，并按收藏时间倒序展示；页面只显示已收藏的原始版或修订版 prompt，支持复制、取消收藏和再次编辑。空收藏场景也已显示明确空状态。
* **🎯 关卡目标**：在收藏页中只展示已收藏的单条 prompt。
* **🛠️ 推荐工具/轮子**：数据库筛选查询、列表组件
* **📦 预计产出**：一个可直接查看收藏 prompt 的页面。
* **🚫 边界限制 (防超纲)**：本关不做搜索，不做排序筛选器。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：收藏至少两条 prompt 后打开收藏页。
  - [x] 预期结果 1：收藏页中只显示已收藏内容，不显示未收藏项。
  - [x] 测试动作 2：取消其中一条收藏并刷新收藏页。
  - [x] 预期结果 2：该条记录从收藏页消失。

### 🟢 Quest 11.4：实现基于历史复制为新版本再编辑
* **📍 当前进度**：已完成。历史页与收藏页中的原始版/任意修订版 prompt 均已提供“再次编辑”入口；实现时明确复用了阶段 7.1 与阶段 10 已验证稳定的 AI 沟通方式，即继续使用 `AI SDK + @ai-sdk/openai-compatible + generateText(...)` 的服务端修订链路，只是把用户选中的历史 prompt 作为新的编辑基底传入。再次编辑不会覆盖旧记录，而是为对应图片新增一个后续修订版本。
* **🎯 关卡目标**：支持从历史记录或收藏页打开一条 prompt，并复制为新版本继续编辑。
* **🛠️ 推荐工具/轮子**：版本创建接口、历史详情页
* **📦 预计产出**：可复用的历史 prompt 再编辑流程。
* **🚫 边界限制 (防超纲)**：本关不直接覆盖旧版本，不做复杂分支管理。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：从历史页选择一条旧 prompt，执行“再次编辑”。
  - [x] 预期结果 1：系统基于旧 prompt 创建一个新的编辑版本入口，而不是覆盖旧记录。
  - [x] 测试动作 2：完成编辑后回看原记录与新记录。
  - [x] 预期结果 2：原记录仍然保留，新版本作为独立记录存在。

## 阶段 12：异常处理与边界情况
本阶段目标是避免产品只在理想路径下工作，确保用户在常见失败场景中也能看懂发生了什么。

### 🟢 Quest 12.1：处理空输入与缺失选择
* **📍 当前进度**：已完成。生成页已补充前端提交前校验：未上传图片时会明确提示“请先选择至少一张图片”；未选择模型配置时会提示“请选择模型配置”；未明确选择助手设计师或“使用系统默认规则”时会提示补全选择，并阻止发起生成请求。为满足本关验收，生成页已改为必须由用户显式选择模型配置与助手规则，而不是默认代选。
* **🎯 关卡目标**：在未上传图片、未选模型配置、未选助手设计师时，给出明确提示并阻止无效提交。
* **🛠️ 推荐工具/轮子**：前端表单状态、提示组件
* **📦 预计产出**：明确友好的前端错误提示。
* **🚫 边界限制 (防超纲)**：本关不处理服务端错误，不做国际化。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：不上传图片直接点击生成。
  - [x] 预期结果 1：页面明确提示需要先上传图片。
  - [x] 测试动作 2：只上传图片但不选择模型配置或助手设计师。
  - [x] 预期结果 2：页面明确提示缺少必要选择，且不会发起生成。

### 🟢 Quest 12.2：处理第三方接口失败
* **📍 当前进度**：已完成。服务端已增强第三方错误映射：可识别 API Key 鉴权失败、Base URL 不可用/网络失败、模型名错误/模型不存在、请求超时，以及图片能力不兼容等常见错误；生成页结果卡片会展示原始失败原因，并补充用户可理解的下一步提示，例如检查 API Key、Base URL、Model 名称或更换支持图片输入的兼容平台。
* **🎯 关卡目标**：当 API Key 错误、Base URL 不可用、模型名错误或接口超时时，页面能显示可理解的失败信息。
* **🛠️ 推荐工具/轮子**：服务端错误映射、统一错误提示组件
* **📦 预计产出**：可读的 AI 调用失败反馈。
* **🚫 边界限制 (防超纲)**：本关不做自动重试，不做报警系统。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：使用错误 API Key 进行一次生成。
  - [x] 预期结果 1：页面提示鉴权或接口调用失败，而不是长时间无响应。
  - [x] 测试动作 2：使用错误模型名进行一次生成。
  - [x] 预期结果 2：页面提示模型配置异常或请求失败，并允许用户返回修改配置。

### 🟢 Quest 12.3：处理历史为空和收藏为空
* **📍 当前进度**：已完成。历史页与收藏页此前已具备明确空状态：无历史任务时显示“暂无历史任务 / 暂无可查看的任务详情”，无收藏内容时显示“暂无收藏内容”，不会让用户误以为页面加载失败。本轮已复核该行为，构建通过，无需额外改动。
* **🎯 关卡目标**：当用户还没有任何历史记录或收藏记录时，页面能显示清晰的空状态。
* **🛠️ 推荐工具/轮子**：空状态组件
* **📦 预计产出**：不会让用户误以为页面坏掉的空状态页面。
* **🚫 边界限制 (防超纲)**：本关不做推荐内容，不做新手引导系统。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：在没有历史数据时打开历史页。
  - [x] 预期结果 1：页面显示“暂无历史记录”之类的清晰提示。
  - [x] 测试动作 2：在没有收藏数据时打开收藏页。
  - [x] 预期结果 2：页面显示“暂无收藏内容”之类的清晰提示。

## 阶段 13：部署上线与访问保护
本阶段目标是把本地可用的工具变成真正可访问的线上应用，并尽量降低敏感配置暴露风险。

### 🟢 Quest 13.1：部署到线上环境
* **📍 当前进度**：已完成。项目已成功部署到 Vercel，线上地址可正常访问；已完成线上环境基本访问验证，并走通至少一次核心生成链路验收。
* **🎯 关卡目标**：将项目部署到 Vercel，并让线上地址可访问。
* **🛠️ 推荐工具/轮子**：Vercel
* **📦 预计产出**：一个可公开访问或私有访问的线上 URL。
* **🚫 边界限制 (防超纲)**：本关不做自定义域名，不做性能优化。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：打开线上 URL。
  - [x] 预期结果 1：页面能正常加载，不是 404、500 或部署默认错误页。
  - [x] 测试动作 2：在线上环境走通一次完整生成流程。
  - [x] 预期结果 2：能够成功上传图片并获得结果，说明线上核心链路可用。

### 🟢 Quest 13.2：配置线上环境变量
* **📍 当前进度**：已完成。已将数据库连接等敏感配置迁移到 Vercel 环境变量中；线上环境访问数据库相关页面与生成链路均可正常工作，代码仓库与前端页面中未暴露明文敏感信息。
* **🎯 关卡目标**：将数据库连接、应用密钥等敏感配置迁移到线上环境变量中。
* **🛠️ 推荐工具/轮子**：Vercel Environment Variables
* **📦 预计产出**：不依赖本地配置文件的线上运行环境。
* **🚫 边界限制 (防超纲)**：本关不处理复杂密钥轮换，不接入专业密钥管理平台。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：在线上环境执行依赖数据库的页面访问或生成功能。
  - [x] 预期结果 1：功能可正常工作，说明线上环境变量配置生效。
  - [x] 测试动作 2：检查代码仓库或前端页面。
  - [x] 预期结果 2：看不到明文数据库连接信息或服务端敏感密钥。

### 🟢 Quest 13.3：配置应用访问保护
* **📍 当前进度**：已完成。已为线上应用增加基础访问保护，未通过访问保护时无法直接进入应用；通过验证后可正常访问和使用主界面，满足单人私有工具的最小保护需求。
* **🎯 关卡目标**：为单人使用场景增加最基础的访问保护，避免工具被任意公开访问。
* **🛠️ 推荐工具/轮子**：部署平台访问保护能力或反向代理访问控制
* **📦 预计产出**：一个受保护的线上入口。
* **🚫 边界限制 (防超纲)**：本关不做站内账号体系，不做多角色权限。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：在未通过访问保护的情况下打开线上地址。
  - [x] 预期结果 1：无法直接进入应用主页。
  - [x] 测试动作 2：通过访问保护后进入应用。
  - [x] 预期结果 2：可以正常看到并使用应用主界面。

## 阶段 14：基础日志与迭代建议
本阶段目标是让项目上线后至少具备最基础的可排查能力，并明确后续升级路径。

### 🟢 Quest 14.1：补充关键流程日志
* **📍 当前进度**：已完成。已为 `/api/generate`、`/api/revisions`、`/api/history`、`/api/favorites` 补充基础服务端日志，覆盖请求开始、关键参数、成功结果与失败原因；结合 Vercel 日志面板，现已可排查生成、修订、历史保存与收藏等关键链路。
* **🎯 关卡目标**：为生成、修订、保存历史、收藏等关键操作增加基础日志。
* **🛠️ 推荐工具/轮子**：服务端日志、Vercel 日志面板
* **📦 预计产出**：可用于排查问题的基础日志信息。
* **🚫 边界限制 (防超纲)**：本关不做专业监控平台，不做链路追踪。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：完成一次生成、一次修订、一次收藏操作后查看日志。
  - [x] 预期结果 1：日志中能看到对应操作的成功记录。
  - [x] 测试动作 2：故意触发一次失败请求后查看日志。
  - [x] 预期结果 2：日志中能看到失败原因或失败阶段，而不是完全没有信息。

### 🟢 Quest 14.2：整理 MVP 之后的迭代清单
* **📍 当前进度**：已完成。已新建 `阶段14-MVP之后的迭代清单.md`，整理搜索、标签、导出、共享助手、多模型对比、移动端优化、更完整日志与大图上传架构升级等后续方向，并补充每项“为什么值得做”的说明。
* **🎯 关卡目标**：将 MVP 完成后的自然升级方向沉淀成可执行的后续清单。
* **🛠️ 推荐工具/轮子**：产品 backlog、版本规划文档
* **📦 预计产出**：一份清晰的后续版本候选列表。
* **🚫 边界限制 (防超纲)**：本关不立即开发新功能，不进行架构大重构。
* **✅ 考官验收标准 (PM Checklist)**：
  - [x] 测试动作 1：查看迭代清单文档。
  - [x] 预期结果 1：能看到至少包括搜索、标签、导出、共享助手、多模型对比、移动端优化等候选方向。
  - [x] 测试动作 2：检查每个候选项的描述。
  - [x] 预期结果 2：每个候选项都能让人理解“为什么值得做”，而不是只有标题没有说明。
