# 阶段 13：Vercel 免费部署操作清单

本文档用于完成 `反推.md` 中的阶段 13：`部署上线与访问保护`。内容按实际操作顺序整理，尽量做到可以边看边执行。

## 目标

- 完成 `13.1`：将项目部署到 Vercel，并拿到可访问的线上 URL。
- 完成 `13.2`：将数据库连接等敏感配置迁移到线上环境变量中。
- 完成 `13.3`：为单人使用场景补上最基础的访问保护。

## 当前项目已确认情况

- 项目是标准 `Next.js` 应用。
- `package.json` 中构建命令为 `next build`。
- 项目中没有 `vercel.json` 特殊部署配置。
- 当前代码里已明确使用 `process.env.DATABASE_URL` 读取数据库连接。
- 项目使用 Prisma + Postgres（当前为 Neon）。
- 部署目标优先采用 `Vercel 免费版 + vercel.app 默认域名`。

## 先准备什么

在开始部署前，先确认以下材料已经准备好：

1. 一个可登录的 `Vercel` 账号。
2. 一个可登录的 `GitHub` 账号。
3. 项目代码已经推送到 Git 仓库，最好是 `GitHub`。
4. 本地执行 `npm run build` 可以通过。
5. 已准备好线上数据库连接串，即 `DATABASE_URL`。
6. 手上有至少一条可用的模型配置和一个可用的助手设计师，用于线上验收。
7. 手上有一张测试图片，用于上线后验证“上传图片 -> 生成 prompt”链路。

## 整体流程

阶段 13 推荐按下面顺序完成：

1. 将代码推送到 GitHub。
2. 在 Vercel 导入该仓库并创建项目。
3. 在 Vercel 中配置环境变量 `DATABASE_URL`。
4. 确保线上数据库已执行 Prisma 迁移。
5. 触发部署，拿到 `vercel.app` 线上地址。
6. 在线上地址验证页面加载和完整生成流程。
7. 最后配置访问保护，并再次验证。

## 详细操作步骤

### 步骤 1：本地最终确认

部署前先在本地做一次最终确认：

1. 执行 `npm run build`。
2. 确认没有构建错误。
3. 确认当前代码就是准备上线的版本。
4. 确认没有把敏感配置硬编码进代码。

通过标准：

- 本地构建通过。
- 本地页面可正常访问。
- 数据库、生成、历史、收藏、修订这些已有功能没有明显回退。

### 步骤 2：把代码推送到 GitHub

如果代码还不在 GitHub：

1. 新建一个 GitHub 仓库。
2. 将当前项目推送到该仓库。
3. 建议把准备上线的分支作为主分支，例如 `main`。

如果代码已经在 GitHub：

1. 确认远程仓库是最新代码。
2. 确认 Vercel 将读取正确分支。

### 步骤 3：在 Vercel 导入项目

操作路径：

1. 打开 `https://vercel.com`。
2. 使用 GitHub 账号登录。
3. 点击 `Add New...`。
4. 选择 `Project`。
5. 选择当前项目对应的 GitHub 仓库。
6. 进入导入页面后，检查 Vercel 自动识别结果。

正常情况下应能自动识别：

- Framework: `Next.js`
- Build Command: `next build`
- Output Settings: 使用默认识别结果即可

当前项目没有 `vercel.json`，通常不需要额外写自定义部署配置。

### 步骤 4：配置线上环境变量

在 Vercel 项目配置中添加环境变量：

- `DATABASE_URL`

建议：

1. 至少配置到 `Production` 环境。
2. 如果后面还想测试预发布环境，也可以同步配置到 `Preview`。

注意事项：

1. 不要把 `DATABASE_URL` 提交回代码仓库。
2. 不要把数据库连接信息暴露到前端页面。
3. 如果后续代码新增别的服务端密钥，也应统一放入 Vercel Environment Variables。

### 步骤 5：处理 Prisma 数据库迁移

这是阶段 13 最容易出问题的点之一。

目标：

- 线上数据库必须具备当前项目需要的完整表结构。
- 至少包括模型配置、助手设计师、生成任务、单图结果、修订版本、收藏相关字段。

推荐做法：先手动执行一次 Prisma 迁移，再部署。

推荐命令：

```bash
npx prisma migrate deploy
```

执行前提：

- 执行这条命令时，环境变量 `DATABASE_URL` 必须指向线上使用的 Neon 数据库。

为什么推荐先手动迁移：

1. 比在 Vercel 构建阶段自动迁移更稳。
2. 一旦迁移失败，可以先排查数据库问题，不会直接让整次部署一起失败。
3. 更适合当前这个 MVP 阶段。

通过标准：

- 线上数据库中已经出现当前 Prisma schema 对应的业务表。
- 没有迁移失败或权限不足错误。

### 步骤 6：触发首次部署

完成环境变量配置后：

1. 在 Vercel 中执行首次部署。
2. 等待构建完成。
3. 拿到一个 `https://xxx.vercel.app` 的线上地址。

首次部署完成后，先做最基础检查：

1. 打开首页。
2. 确认不是 `404`、`500` 或默认部署错误页。
3. 尝试进入生成页、模型配置页、助手设计师页、历史页、收藏页。

如果出现报错，优先排查：

1. `DATABASE_URL` 是否填错。
2. Prisma 迁移是否已执行。
3. 线上数据库是否允许当前连接。

### 步骤 7：执行阶段 13 的线上验收

部署成功后，按下面顺序做线上验收。

#### 13.1 验收：线上地址可访问

1. 打开 `vercel.app` 地址。
2. 确认首页能加载。
3. 确认页面不是空白页、404、500 或部署错误页。

#### 13.2 验收：线上环境变量生效

1. 打开依赖数据库的页面，例如模型配置页、助手设计师页、历史页、收藏页。
2. 确认页面能正常读取数据库内容。
3. 确认代码仓库和前端页面都看不到明文数据库连接信息。

#### 核心业务链路验收

按下面流程走通一次完整生成流程：

1. 打开生成页。
2. 上传一张测试图片。
3. 选择一个模型配置。
4. 选择一个助手设计师，或明确选择系统默认规则。
5. 点击生成。
6. 确认页面返回 prompt 结果。
7. 打开历史页，确认历史记录已经保存。
8. 测试复制按钮。
9. 测试收藏按钮。
10. 测试继续修改并生成新修订版本。

通过标准：

- 页面可访问。
- 数据库读取正常。
- 上传图片后能够生成 prompt。
- 历史、收藏、修订链路在线上可用。

### 步骤 8：配置访问保护

阶段 13 的目标之一是避免工具被任意公开访问。

当前推荐策略：

1. 先完成 Vercel 免费部署。
2. 再补“最小可用的访问保护”。

对于当前单人使用项目，优先考虑：

- 项目内实现一个轻量密码门禁
- 或使用部署平台本身提供的基础访问保护能力

注意边界：

- 不做站内账号体系。
- 不做多角色权限。
- 不把阶段 13 扩展成完整登录系统。

访问保护验收标准：

1. 未通过访问保护时，不能直接进入应用首页。
2. 通过访问保护后，可以正常看到并使用应用主界面。

## 关键点与高频风险

### 关键点 1：环境变量必须走 Vercel 配置

线上运行不能依赖本地 `.env` 文件。

当前至少要保证：

- `DATABASE_URL` 已正确配置到 Vercel

### 关键点 2：数据库迁移必须落实到线上库

只是在本地执行过迁移，不代表线上数据库已经有表结构。

必须确认：

- 线上库已执行 `prisma migrate deploy`
- 线上库表结构与当前代码一致

### 关键点 3：真正的成本更可能来自 AI 调用

Vercel 免费部署和数据库免费额度通常足够当前 MVP 验证，但第三方模型接口调用会真实产生费用。

尤其是：

- 图片分析
- 多图生成
- 连续修订

这些都会消耗模型 API 额度。

### 关键点 4：访问保护要保持最小化

当前项目目标是单人使用工具。

所以访问保护只需要满足：

- 不让陌生人直接打开即用
- 自己授权后能正常进入

不要在阶段 13 引入完整账号系统。

## 哪些事情必须由你亲自完成

以下事项通常需要你本人操作：

1. 登录 `GitHub` 与 `Vercel`。
2. 创建或选择 GitHub 仓库。
3. 在 Vercel 中导入仓库并创建项目。
4. 在 Vercel 控制台中填写 `DATABASE_URL`。
5. 确认线上使用哪一个 Neon 数据库。
6. 点击触发部署。
7. 最终确认线上 URL 是否可访问。
8. 决定访问保护采用哪种方式。

## 可以由协作方继续协助的事情

以下事项可以继续由开发协作补充：

1. 检查 Prisma 配置是否最适合 Vercel 部署。
2. 检查是否还需要补充部署相关脚本或说明。
3. 检查环境变量清单是否完整。
4. 补充项目内的最小访问保护实现。
5. 协助排查首次部署报错、数据库连接报错、生成链路报错。

## 最终执行清单

可直接按下面顺序逐项打勾执行：

- [ ] 本地 `npm run build` 通过
- [ ] 代码已推送到 GitHub
- [ ] 在 Vercel 成功导入仓库
- [ ] 已在 Vercel 配置 `DATABASE_URL`
- [ ] 已对线上数据库执行 `npx prisma migrate deploy`
- [ ] Vercel 首次部署成功
- [ ] `vercel.app` 地址可正常打开
- [ ] 线上模型配置/助手/历史等数据库页面可正常访问
- [ ] 已在线上完成一次上传图片并生成 prompt
- [ ] 已验证历史保存成功
- [ ] 已验证复制/收藏/修订链路可用
- [ ] 已配置访问保护
- [ ] 已验证“未授权不能进入，授权后可正常使用”

## 阶段 13 完成判定

当以下条件全部满足时，可认为阶段 13 完成：

1. 有一个可访问的线上 URL。
2. 线上环境变量配置完成且敏感信息未暴露。
3. 线上数据库可正常读写。
4. 上传图片生成 prompt 的主链路在线上可用。
5. 应用入口已具备基础访问保护。
