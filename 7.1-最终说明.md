# Quest 7.1 最终说明

## 1. 最终结论
- `Quest 7.1` 已通过人工验收。
- 用户已确认：
  - 单图生成可正常返回反推 prompt
  - 多图生成链路也已可正常工作
- 当前阻塞已解除，`7.1` 可视为完成。

## 2. 问题是如何定位并解决的
### 初始现象
- 页面最早报错为：`403 Your request was blocked.`
- 一度怀疑：
  - 当前模型配置不可用
  - 兼容平台不支持图片
  - 权限/风控/视觉能力存在问题

### 逐步排查后的关键结论
1. 文本链路实际上是通的：
   - `GET /v1/models` 成功
   - 纯文本 `chat.completions` 成功
   - 纯文本 `responses` 成功
2. 因此问题收敛到图片/多模态请求层，而不是整套配置完全不可用。
3. 用户补充了关键线索：
   - 同站点同模型在其他 AI 终端中可识别图片
   - 在 OpenCode 中也可识别图片
   - 配置该站点时需要选 **OpenAI Responses** 类型
4. 由此判断：
   - 不是“平台绝对不支持图片”
   - 更可能是当前项目的调用路径与图片输入格式不匹配

## 3. 最终成功方案
### 方案核心
当前成功方案不是继续手写 OpenAI `responses.create(...)` 请求体，而是改成：
- `AI SDK`
- `@ai-sdk/openai-compatible`
- 图片以 `Uint8Array + mediaType` 形式传入

### 为什么之前失败
之前失败主要有两层原因：
1. 调用路径不够接近 OpenCode，兼容站点对手写 OpenAI Responses 图片请求不稳定。
2. 在 AI SDK 路线下，图片不能直接继续传 `data:image/...` URL。

### 关键运行时发现
在第一次切到 AI SDK 后，页面报错：
- `URL scheme must be http or https, got data:`

这说明：
- 错误发生在 AI SDK / provider 层的输入校验阶段
- 请求还没有成功发到上游兼容平台
- AI SDK 这条路径下，不能把图片作为 `data:` URL 传给 image part

### 最终修正
最终改为：
- 服务端接收上传图片
- 将图片转成 `Uint8Array`
- 同时携带 `mediaType`
- 用 `createOpenAICompatible(...)` 创建 provider
- 使用 `generateText(...)` 发送 system + user 图文消息
- 返回模型生成的 prompt 文本

## 4. 当前代码实现概览

### 4.1 `app/api/generate/route.ts`
作用：`Quest 7.1` 的主入口接口。

关键职责：
- 接收 `FormData`
- 校验 `image / configId / assistantId`
- 从数据库读取模型配置与助手设计师
- 将上传图片转成二进制：
  - `Uint8Array`
  - `mimeType`
- 调用 `analyzeImageWithResponses(...)`
- 返回：
  - 成功：`{ ok: true, prompt }`
  - 失败：`{ ok: false, error }`

关键定位：
- `app/api/generate/route.ts:18`
- `app/api/generate/route.ts:49`
- `app/api/generate/route.ts:57`

### 4.2 `lib/ai.ts`
作用：AI 调用封装核心。

关键职责：
- `createCompatibleProvider(...)`：创建 OpenAI-compatible provider
- `analyzeImageWithResponses(...)`：当前图片分析主实现
- 使用：
  - `generateText`
  - `ModelMessage`
  - `type: "image"` 的图片 part
- 图片输入格式为：
  - `image: Uint8Array`
  - `mediaType: string`
- `mapAIError(...)` 负责统一错误映射

关键定位：
- `lib/ai.ts:11`
- `lib/ai.ts:49`
- `lib/ai.ts:85`
- `lib/ai.ts:111`

### 4.3 `lib/prompt-template.ts`
作用：system prompt 组装逻辑。

关键职责：
- `buildSystemPrompt(...)` 根据：
  - 助手设计师 `systemPrompt`
  - 助手设计师 `outputTemplate`
  - 默认系统规则
  生成最终 system prompt
- 当前 `Quest 7.1` 的输出结构与风格控制仍依赖这里
- 文件中旧的 `buildImageMessage(...)` 仍是早期 Chat Completions / data URL 思路的遗留参考，不是当前成功链路的核心入口

关键定位：
- `lib/prompt-template.ts:14`
- `lib/prompt-template.ts:53`
- `lib/prompt-template.ts:76`

### 4.4 `app/generate/generate-client.tsx`
作用：生成页前端交互与结果展示。

关键职责：
- 上传图片
- 选择模型配置和助手设计师
- 调用 `/api/generate`
- 成功时展示 prompt 并提供复制按钮
- 失败时展示服务端错误文案

关键定位：
- `app/generate/generate-client.tsx:198`
- `app/generate/generate-client.tsx:217`
- `app/generate/generate-client.tsx:230`

### 4.5 `package.json`
作用：记录当前已验证可工作的依赖版本组合。

关键依赖：
- `ai@5.0.162`
- `@ai-sdk/openai-compatible@1.0.34`

说明：
- 这是当前已验证能编译并支撑本次实现的版本线。

关键定位：
- `package.json:11`
- `package.json:12`
- `package.json:16`

## 5. 当前成功链路的关键经验
1. 不要再回到“图片转 base64 data URL + 直接塞给 AI SDK image part”的做法。
   - 这会触发：`URL scheme must be http or https, got data:`
2. 当前兼容平台下，更稳定的方案是：
   - AI SDK provider 路线
   - 二进制图片输入
3. 当前链路已经足够支撑：
   - 单图生成
   - 多图生成

## 6. 相关风险与注意事项
- 在早期排查中曾发现页面向前端暴露 `apiKey` 的风险，这不是 `Quest 7.1` 本身的阻塞点，但属于需要后续单独处理的安全问题。
- 如果后续再次出现某些兼容平台的图片异常，优先排查：
  1. 是否误把图片又转回 `data:` URL
  2. 当前 provider 版本是否被改动
  3. 当前模型配置是否确实支持图片输入
  4. 是否需要 provider 级别定制（如 `transformRequestBody`）

## 7. 建议的后续动作
- 保留当前 AI SDK + OpenAI-compatible provider 的实现路径，作为后续图片能力的基础方案
- 可继续把本次成功链路推广到：
  - 多图正式任务
  - 继续修改
  - 历史入库
- 若后续还要继续推进验收，优先进入 `8.x` 阶段
