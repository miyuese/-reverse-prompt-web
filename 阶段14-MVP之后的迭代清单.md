## 阶段 14：MVP 之后的迭代清单

本文档用于整理当前 MVP 完成后的自然升级方向。目标不是立刻全部开发，而是把后续版本候选项按价值和动机说明清楚，便于后续排期。

## 一、优先级较高

### 1. 历史记录搜索

为什么值得做：

当前历史页只能按时间浏览，随着生成任务变多，用户很难快速定位某次任务、某类 prompt 或某张图片。增加搜索后，历史记录会从“堆积结果”变成“可复用资产”。

建议范围：

1. 支持按模型配置名称搜索
2. 支持按助手名称搜索
3. 支持按文件名搜索
4. 支持按 prompt 关键词搜索

### 2. 收藏标签体系

为什么值得做：

当前收藏页只能展示“已收藏”和“未收藏”，当收藏数量增长后，很难按主题管理。标签能让收藏真正具备整理价值。

建议范围：

1. 收藏条目支持添加多个标签
2. 收藏页支持按标签筛选
3. 先做简单自由输入，不必一开始上复杂管理后台

### 3. Prompt 导出能力

为什么值得做：

当前用户主要依赖复制按钮单条复制，适合少量使用，但不适合批量整理或外部备份。导出功能可以提升 prompt 的可迁移性和长期保存能力。

建议范围：

1. 导出单次任务的全部 prompt
2. 导出收藏页中的已收藏 prompt
3. 导出格式优先支持 `.txt` 和 `.json`

### 4. 移动端体验优化

为什么值得做：

当前页面虽然已具备基础响应式布局，但移动端在多图结果、长 prompt、历史页详情和按钮密集区上的体验仍有提升空间。随着真实使用增多，这会明显影响可用性。

建议范围：

1. 优化生成页结果卡片在窄屏下的排版
2. 优化历史页和收藏页的长文本阅读体验
3. 增强按钮区在移动端的可点击性

## 二、中期可做

### 5. 多模型对比生成

为什么值得做：

当前一次生成只能使用一个模型配置。如果后续希望比较不同平台或不同模型对同一张图片的反推能力，这会是很自然的升级方向。

建议范围：

1. 同一张图支持选择多个模型配置
2. 结果区按模型维度分组展示
3. 保持逐图独立，不做复杂并发优化作为第一版目标

### 6. 助手模板复制与共享

为什么值得做：

当前助手设计师已经支持增删改，但复用方式仍然比较基础。复制模板能让试错成本更低，也便于快速迭代不同风格规则。

建议范围：

1. 助手设计师支持一键复制为新模板
2. 后续如果需要，可考虑导入导出模板 JSON

### 7. 更完整的日志和监控能力

为什么值得做：

阶段 14.1 只是补上了基础日志，能帮助人工排障，但还不具备系统化观测能力。随着线上使用增加，日志结构化和错误追踪会变得更重要。

建议范围：

1. 记录生成与修订耗时
2. 记录图片压缩前后体积
3. 记录失败类型统计
4. 后续可接入更专业的监控平台

### 8. 大图上传架构升级

为什么值得做：

当前已通过浏览器端压缩解决了 Vercel `413`，这对 MVP 足够有效，但如果未来要支持更高分辨率图片、更复杂场景或更大批量上传，现有直传函数模式会再次碰到平台限制。

建议范围：

1. 引入对象存储保存临时图片
2. 后端处理图片 URL，而不是原始大文件体
3. 继续保持“不长期保存原图”的业务边界

## 三、较后期再考虑

### 9. 收藏与历史的高级筛选

为什么值得做：

当数据规模扩大后，仅靠搜索不一定足够。按时间、模型、助手、是否修订、是否收藏等条件组合筛选，会明显提升内容管理效率。

建议范围：

1. 时间范围筛选
2. 模型配置筛选
3. 助手设计师筛选
4. 原始版 / 修订版筛选

### 10. 站内轻量账号体系或更细的访问控制

为什么值得做：

当前产品定位是单人私有工具，使用部署层访问保护已经足够。但如果后续出现跨设备、多人共用或模板共享需求，访问控制会成为自然扩展点。

建议范围：

1. 保持当前单人前提不变，不提前过度设计
2. 真正有多人需求时，再考虑最小账号体系或更细粒度权限控制

## 四、建议执行顺序

结合当前产品状态，建议优先级如下：

1. 历史记录搜索
2. 收藏标签体系
3. Prompt 导出能力
4. 移动端体验优化
5. 多模型对比生成
6. 更完整的日志和监控能力
7. 大图上传架构升级

## 五、一句话总结

MVP 已经完成了从图片上传、AI 生成、历史保存、修订、收藏到线上部署的闭环。下一阶段最值得做的，不是盲目扩功能，而是优先提升“可检索、可整理、可导出、可扩展”的能力，让这套工具从可用变成更稳定、更耐用。
